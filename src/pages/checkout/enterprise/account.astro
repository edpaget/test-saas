---
import CheckoutLayout from "../../../layouts/CheckoutLayout.astro";
import FormField from "../../../components/FormField.astro";
import Button from "../../../components/Button.astro";
---

<CheckoutLayout title="Account Details" planName="Enterprise" planSlug="enterprise" planPrice={99} currentStep={2}>
  <h1 class="text-2xl font-bold text-gray-900">Account Details</h1>
  <p class="mt-2 text-gray-500">Create your FlowDesk account to get started.</p>

  <form class="mt-8 space-y-6" onsubmit="event.preventDefault()">
    <div class="grid gap-6 sm:grid-cols-2">
      <FormField label="First Name" name="firstName" placeholder="Jane" required />
      <FormField label="Last Name" name="lastName" placeholder="Doe" required />
    </div>
    <FormField label="Work Email" name="email" type="email" placeholder="jane@company.com" required />
    <FormField label="Company Name" name="company" placeholder="Acme Inc." required />
    <FormField label="Team Size" name="teamSize" placeholder="e.g. 50" />
    <FormField label="Job Title" name="jobTitle" placeholder="e.g. CTO" />
    <FormField label="Password" name="password" type="password" placeholder="At least 8 characters" required />

    <div class="flex gap-4">
      <Button href="/checkout/enterprise/plan" variant="secondary">Back</Button>
      <Button
        href="/checkout/enterprise/payment"
        variant="primary"
        data-umami-event="checkout-submit-account"
        data-umami-event-plan="enterprise"
      >
        Continue to Payment
      </Button>
    </div>
  </form>
</CheckoutLayout>

<script>
  window.umami?.track("checkout-view-account", { plan: "enterprise" });
</script>
